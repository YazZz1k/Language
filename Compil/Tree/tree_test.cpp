#include"tree.hpp"
#include<iostream>

/*int main()
{
    token tok("push", OP);
    Tree* tree = Create_Tree(tok);
    for(int i=0; i<5; i++)
        tree->Add_Child(Create_Tree(tok));

    for(int i=0; i<4; i++)
        (*tree)[0]->Add_Child(Create_Tree(tok));

    tok.value = "21212";
    tree->print_DOT();
    tree->~Tree();
}*/
